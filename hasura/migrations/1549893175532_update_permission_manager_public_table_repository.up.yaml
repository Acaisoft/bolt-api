- args:
    permission:
      allow_upsert: true
      check:
        project:
          userProjects:
            user_id:
              _eq: X-HASURA-USER-ID
      columns:
      - name
      - password
      - project_id
      - url
      - user_id
      - username
      set: {}
    role: manager
    table:
      name: repository
      schema: public
  type: create_insert_permission

- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."execution_parameter"("execution_id" uuid NOT NULL,
      "parameter_id" uuid NOT NULL, "id" uuid NOT NULL UNIQUE DEFAULT gen_random_uuid(),
      PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: execution_parameter
    schema: public
  type: add_existing_table_or_view

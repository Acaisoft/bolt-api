- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."execution_users"("execution_id" uuid NOT NULL, "timestamp"
      timestamptz NOT NULL DEFAULT now(), "users_count" integer NOT NULL DEFAULT 0,
      "instance_id" uuid NOT NULL, "id" uuid NOT NULL UNIQUE DEFAULT gen_random_uuid(),
      PRIMARY KEY ("id") , FOREIGN KEY ("execution_id") REFERENCES "execution"("id")
      ON UPDATE restrict ON DELETE cascade);
  type: run_sql
- args:
    name: execution_users
    schema: public
  type: add_existing_table_or_view

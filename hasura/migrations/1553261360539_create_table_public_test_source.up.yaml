- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."test_source"("id" uuid NOT NULL UNIQUE DEFAULT gen_random_uuid(),
      "project_id" uuid NOT NULL, "source_type" text NOT NULL DEFAULT 'repository',
      "repository_id" uuid , "test_creator_id" uuid , PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: test_source
    schema: public
  type: add_existing_table_or_view

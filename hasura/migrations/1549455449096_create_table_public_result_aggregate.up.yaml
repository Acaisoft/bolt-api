- args:
    sql: CREATE TABLE "public"."result_aggregate"("id" bigserial NOT NULL UNIQUE,
      "timestamp" bigint NOT NULL, "fail" integer NOT NULL DEFAULT 0, "av_resp_time"
      numeric NOT NULL, "succes" integer NOT NULL, "error" integer NOT NULL, "av_size"
      numeric NOT NULL, "execution_id" uuid NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: result_aggregate
    schema: public
  type: add_existing_table_or_view

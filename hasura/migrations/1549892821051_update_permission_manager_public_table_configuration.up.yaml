- args:
    permission:
      columns:
      - name
      filter:
        project:
          userProjects:
            user_id:
              _eq: X-HASURA-USER-ID
    role: manager
    table:
      name: configuration
      schema: public
  type: create_update_permission

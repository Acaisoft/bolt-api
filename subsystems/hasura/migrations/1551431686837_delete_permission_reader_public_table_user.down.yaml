- args:
    permission:
      allow_aggregations: false
      columns:
      - id
      - email
      - active
      - created
      - is_admin
      - is_reader
      - is_manager
      filter:
        id:
          _eq: X-HASURA-USER-ID
    role: reader
    table:
      name: user
      schema: public
  type: create_select_permission

- args:
    sql: CREATE TABLE "public"."argo_execution_log"("id" serial NOT NULL, "data" json
      NOT NULL, "created_at" timestamp with time zone NOT NULL DEFAULT now(), PRIMARY
      KEY ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: argo_execution_log
    schema: public
  type: add_existing_table_or_view

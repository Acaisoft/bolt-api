- args:
    permission:
      columns:
      - name
      - password
      - url
      - username
      filter:
        project:
          userProjects:
            user_id:
              _eq: X-HASURA-USER-ID
    role: manager
    table:
      name: repository
      schema: public
  type: create_update_permission

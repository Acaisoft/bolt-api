- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."user_registration_token"("token" uuid NOT NULL DEFAULT
      gen_random_uuid(), "project_id" uuid NOT NULL, "user_role" text NOT NULL DEFAULT
      'reader', PRIMARY KEY ("token") , FOREIGN KEY ("project_id") REFERENCES "public"."project"("id")
      ON UPDATE cascade ON DELETE cascade, UNIQUE ("token"));
  type: run_sql
- args:
    name: user_registration_token
    schema: public
  type: add_existing_table_or_view

- args:
    sql: CREATE TABLE "public"."execution_errors"("id" serial NOT NULL UNIQUE, "execution_id"
      uuid NOT NULL, "timestamp" timestamp with time zone NOT NULL, "identifier" text
      NOT NULL, "method" text NOT NULL, "name" text NOT NULL, "exception_data" text
      , "number_of_occurrences" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY
      ("execution_id") REFERENCES "public"."execution"("id") ON UPDATE cascade ON
      DELETE cascade);
  type: run_sql
- args:
    name: execution_errors
    schema: public
  type: add_existing_table_or_view

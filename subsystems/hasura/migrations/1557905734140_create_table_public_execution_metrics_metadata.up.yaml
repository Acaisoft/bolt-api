- args:
    sql: CREATE TABLE "public"."execution_metrics_metadata"("id" serial NOT NULL UNIQUE,
      "execution_id" uuid NOT NULL, "chart_configuration" json NOT NULL, PRIMARY KEY
      ("id") , FOREIGN KEY ("execution_id") REFERENCES "public"."execution"("id")
      ON UPDATE cascade ON DELETE cascade);
  type: run_sql
- args:
    name: execution_metrics_metadata
    schema: public
  type: add_existing_table_or_view

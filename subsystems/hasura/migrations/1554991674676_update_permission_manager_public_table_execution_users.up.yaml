- args:
    permission:
      allow_aggregations: false
      columns: []
      filter:
        _and:
        - execution:
            configuration:
              project:
                userProjects:
                  user_id:
                    _eq: X-Hasura-User-Id
        - execution:
            configuration:
              is_deleted:
                _eq: false
        - execution:
            configuration:
              project:
                is_deleted:
                  _eq: false
      limit: null
    role: manager
    table:
      name: execution_users
      schema: public
  type: create_select_permission

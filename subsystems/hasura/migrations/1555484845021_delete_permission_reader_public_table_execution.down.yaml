- args:
    permission:
      allow_aggregations: false
      columns:
      - id
      - configuration_id
      - start
      - end
      - status
      filter:
        configuration:
          project:
            userProjects:
              user_id:
                _eq: X-HASURA-USER-ID
    role: reader
    table:
      name: execution
      schema: public
  type: create_select_permission

- args:
    sql: CREATE TABLE "public"."execution_result"("id" serial NOT NULL UNIQUE, "execution_id"
      uuid NOT NULL, "request_type" text NOT NULL, "endpoint" text NOT NULL, "response_time"
      numeric NOT NULL, "response_length" numeric NOT NULL, "status" text NOT NULL,
      "exception" text NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: execution_result
    schema: public
  type: add_existing_table_or_view

- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."execution_export_token"("id" uuid NOT NULL UNIQUE
      DEFAULT gen_random_uuid(), "execution_id" uuid NOT NULL, "created_by_id" uuid
      , "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: execution_export_token
    schema: public
  type: add_existing_table_or_view
